#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

# "nocheck" option prevents from running tests (which fails... FIXME)
#export DEB_BUILD_OPTIONS=nocheck

%:
	dh $@ 
